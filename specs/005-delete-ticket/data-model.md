# Data Model: 티켓 삭제 (DELETE /api/tickets/:id)

이 기능은 `tickets` 테이블에서 `id`가 일치하는 행을 제거하는 것 외에 데이터 모델을 추가·
변경하지 않는다.

| 조건 | 결과 |
|------|------|
| `id`에 해당하는 티켓이 존재 | 행 삭제, 204 |
| `id`에 해당하는 티켓이 없음 | NOT_FOUND(404) |
